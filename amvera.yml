meta:
  environment: node
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  artifacts:
    "*": /
run:
  command: npm run build && npm run prod
  persistenceMount: /data
  containerPort: 3000
